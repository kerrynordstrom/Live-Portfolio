import React from "react";

import Education from "./Education";
import Work from "./Work";

const Resume = () => (
  <section id="resume">
    <Work />
    <Education />
  </section>
);

export default Resume;
